# Extra dependencies for the example notebooks in examples/.
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-notebooks.txt
#
# Kept separate from requirements.txt so the core pipeline install stays
# lightweight for anyone who only wants Functions.py/MLForecasting.py and has
# no interest in the notebooks (see examples/ and SAMPLE_DATA.md).

jupyter==1.1.1
ipykernel==6.30.1
nbformat==5.10.4
